<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gamma.rechealth">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecHealth">
        <activity android:name=".ui.ConfirmCheckup"></activity>
        <activity android:name=".ui.ProfileActivity" />

        <receiver
            android:name=".helper.OTPReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.auth.api.phone.SMS_RETRIEVED" />
            </intent-filter>
        </receiver>

        <activity android:name=".JadwalActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".HomeDokterActivity" />
        <activity
            android:name=".DaftarRiwayatPenyakitActivity"
            android:label="@string/title_activity_daftar_riwayat_penyakit"
            android:theme="@style/Theme.RecHealth.NoActionBar" />
        <activity android:name=".ui.SearchActivity" />
        <activity
            android:name=".OnBoardingActivity"
            android:label="@string/title_activity_on_boarding" />
        <activity android:name=".ui.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.HomePasien" />
        <activity android:name=".ui.DetailArtikel" />
        <activity android:name=".ui.FormDaftarCheckUp" />
        <activity android:name=".ui.FormCatatanMedis" />
        <activity android:name=".HalamanAwal" />
        <activity android:name=".ui.VerifikasiActivity" />
        <activity android:name=".DetailRiwayatPenyakit" />
        <activity android:name=".DetailPasienActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.RegisterActivity" />
    </application>

</manifest>